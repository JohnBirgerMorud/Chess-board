#include "Player.h"

Player::Player(Color initColor, Playervalue initValue, bool initTurn, string initName) {
    color = initColor;
    value = initValue;
    turn = initTurn;
    name = initName;
}


